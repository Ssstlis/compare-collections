mongodb {

  # Default connection — used when --host1 / --host2 are not specified.
  default {
    host              = "localhost"
    port              = 27017
    user              = ""
    password          = ""
    database          = "mydb"
    maxPoolSize       = 10
    waitQueueMultiple = 2
    zoneId            = "UTC"
  }

  prod {
    user: "admin"
    password: "123"
    database: "main"
    host: "prod.com"
    port: 27017
    maxPoolSize: 100
    waitQueueMultiple: 30
  }

  test {
    user: "admin"
    password: "123"
    database: "main"
    host: "test.com"
    port: 27017
    maxPoolSize: 100
    waitQueueMultiple: 30
  }

}
